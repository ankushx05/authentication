// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v6.32.1
// source: email/v1/email.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "email.v1";

export enum EmailTemplate {
  EMAIL_TEMPLATE_UNSPECIFIED = 0,
  EMAIL_TEMPLATE_REGISTRATION_SUCCESS = 1,
  UNRECOGNIZED = -1,
}

export function emailTemplateFromJSON(object: any): EmailTemplate {
  switch (object) {
    case 0:
    case "EMAIL_TEMPLATE_UNSPECIFIED":
      return EmailTemplate.EMAIL_TEMPLATE_UNSPECIFIED;
    case 1:
    case "EMAIL_TEMPLATE_REGISTRATION_SUCCESS":
      return EmailTemplate.EMAIL_TEMPLATE_REGISTRATION_SUCCESS;
    case -1:
    case "UNRECOGNIZED":
    default:
      return EmailTemplate.UNRECOGNIZED;
  }
}

export function emailTemplateToJSON(object: EmailTemplate): string {
  switch (object) {
    case EmailTemplate.EMAIL_TEMPLATE_UNSPECIFIED:
      return "EMAIL_TEMPLATE_UNSPECIFIED";
    case EmailTemplate.EMAIL_TEMPLATE_REGISTRATION_SUCCESS:
      return "EMAIL_TEMPLATE_REGISTRATION_SUCCESS";
    case EmailTemplate.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface RegistrationSuccessProps {
  fullName: string;
  email: string;
  userName: string;
}

export interface EmailMessage {
  to: string;
  template: EmailTemplate;
  registrationSuccess?: RegistrationSuccessProps | undefined;
}

function createBaseRegistrationSuccessProps(): RegistrationSuccessProps {
  return { fullName: "", email: "", userName: "" };
}

export const RegistrationSuccessProps: MessageFns<RegistrationSuccessProps> = {
  encode(message: RegistrationSuccessProps, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.fullName !== "") {
      writer.uint32(10).string(message.fullName);
    }
    if (message.email !== "") {
      writer.uint32(18).string(message.email);
    }
    if (message.userName !== "") {
      writer.uint32(26).string(message.userName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegistrationSuccessProps {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegistrationSuccessProps();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.fullName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.userName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegistrationSuccessProps {
    return {
      fullName: isSet(object.fullName)
        ? globalThis.String(object.fullName)
        : isSet(object.full_name)
        ? globalThis.String(object.full_name)
        : "",
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      userName: isSet(object.userName)
        ? globalThis.String(object.userName)
        : isSet(object.user_name)
        ? globalThis.String(object.user_name)
        : "",
    };
  },

  toJSON(message: RegistrationSuccessProps): unknown {
    const obj: any = {};
    if (message.fullName !== "") {
      obj.fullName = message.fullName;
    }
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.userName !== "") {
      obj.userName = message.userName;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegistrationSuccessProps>, I>>(base?: I): RegistrationSuccessProps {
    return RegistrationSuccessProps.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegistrationSuccessProps>, I>>(object: I): RegistrationSuccessProps {
    const message = createBaseRegistrationSuccessProps();
    message.fullName = object.fullName ?? "";
    message.email = object.email ?? "";
    message.userName = object.userName ?? "";
    return message;
  },
};

function createBaseEmailMessage(): EmailMessage {
  return { to: "", template: 0, registrationSuccess: undefined };
}

export const EmailMessage: MessageFns<EmailMessage> = {
  encode(message: EmailMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.to !== "") {
      writer.uint32(10).string(message.to);
    }
    if (message.template !== 0) {
      writer.uint32(16).int32(message.template);
    }
    if (message.registrationSuccess !== undefined) {
      RegistrationSuccessProps.encode(message.registrationSuccess, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EmailMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEmailMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.to = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.template = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.registrationSuccess = RegistrationSuccessProps.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EmailMessage {
    return {
      to: isSet(object.to) ? globalThis.String(object.to) : "",
      template: isSet(object.template) ? emailTemplateFromJSON(object.template) : 0,
      registrationSuccess: isSet(object.registrationSuccess)
        ? RegistrationSuccessProps.fromJSON(object.registrationSuccess)
        : isSet(object.registration_success)
        ? RegistrationSuccessProps.fromJSON(object.registration_success)
        : undefined,
    };
  },

  toJSON(message: EmailMessage): unknown {
    const obj: any = {};
    if (message.to !== "") {
      obj.to = message.to;
    }
    if (message.template !== 0) {
      obj.template = emailTemplateToJSON(message.template);
    }
    if (message.registrationSuccess !== undefined) {
      obj.registrationSuccess = RegistrationSuccessProps.toJSON(message.registrationSuccess);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EmailMessage>, I>>(base?: I): EmailMessage {
    return EmailMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EmailMessage>, I>>(object: I): EmailMessage {
    const message = createBaseEmailMessage();
    message.to = object.to ?? "";
    message.template = object.template ?? 0;
    message.registrationSuccess = (object.registrationSuccess !== undefined && object.registrationSuccess !== null)
      ? RegistrationSuccessProps.fromPartial(object.registrationSuccess)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
